/**
 * Spatial dot demo — channel/endpoint constants (client-safe).
 */

export const DOT_CHANNEL_ID = 'dot';

export const SPATIAL_STREAM_ENDPOINT = '/api/spatial/stream';
export const SPATIAL_API_BASE = '/api/spatial';
