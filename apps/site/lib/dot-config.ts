/**
 * Dot demo — channel/endpoint constants (client-safe).
 */

export const DOT_CHANNEL_ID = 'dot';

export const DOT_STREAM_ENDPOINT = '/api/dot/stream';
export const DOT_API_BASE = '/api/dot';

/** Demo override — library default is 5. */
export const DOT_MAX_SCREENS = 4;

/** Active content layer for this demo room — set via SpatialServer.initialMeta. */
export const DOT_CONTENT_LAYER_ID = 'dot';
